<aside class="menu">
    <p class="menu-label">
      Settings
    </p>
    
    <ul class="menu-list">
      <li>
        <a
          class="{{ request.match(['/settings/account']) ? 'is-active' : '' }}"
          href="{{ route('settings.account') }}">Edit Account</a>
      </li>
      <li>
        <a
          class="{{ request.match(['/settings/password']) ? 'is-active' : '' }}"
          href="{{ route('settings.password') }}">Change Password</a>
      </li>
    </ul>
  </aside>
  